import java.util.*;
/**
 * Write a description of class Matrix90 here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Matrix90
{
    int[][]  arr,transform;
    void init(){
        arr=new int[3][3];
        transform=new int[3][3];
        Scanner sc=new Scanner(System.in);
        System.out.println("Enter 9 elements");
        for(int i = 0;i<=arr.length-1;i++){
            for(int j=0;j<=arr[i].length-1;i++){
                arr[i][j]=sc.nextInt();
            }
        }
    }

    void calc(){
        for(int i = 0;i<=arr.length-1;i++){
            for(int j=0;j<=arr[i].length-1;i++){
                arr[i][j]
            }
        }
    }

    public static void main(){

    }
}
